/** Wzór programu wyświetlany w laboratorium — uczeń przepisuje go ręcznie do pola kodu. */
export const LAB_REFERENCE_CODE = `wiek = int(input("Podaj wiek: "))
if wiek >= 18:
    print("Dostęp przyznany")
else:
    print("Brak dostępu")`
